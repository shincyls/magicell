<h4 class="text-left text-success">Step 3: Submit Manager Approval for Timesheet <%= @timesheet.year %>-<%= @timesheet.month %></h4>

<% if @timesheet.submitted %>
    <p class="text-dark text-special">This timesheet has been submitted.</p>
<% else %>
    <br>
    <p class="text-dark text-special">This timesheet has not submit yet, upon submit it will auto send to corresponding project manager.</p>
    <%= button_to "Submit Timesheet", submit_timesheet_path(@timesheet), method: "post", class: "btn btn-success", remote: true, data: { confirm: 'Please confirm, once submitted You cannot edit the data anymore.' }  %>
<% end %>

<br>

<h4 class="text-left text-primary">Approval List</h4>
<div id="timesheet-task-table">
    <%= render partial: "timesheets/approvaltable", local: @timesheet %>
</div>