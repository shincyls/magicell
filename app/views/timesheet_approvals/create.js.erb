$('#flash-message').html("<%= j (render 'statics/message') %>");
$('#monthly-selection').html("<%= j (render 'timesheets/month') %>");
$('#monthly-approval').html("<%= j (render 'timesheets/table') %>");