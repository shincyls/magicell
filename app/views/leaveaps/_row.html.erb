            <% l = @leaveap %>
            <td><%=l.leavetype.name%></td>
            <td><%=l.reason%></td>
            <td><%=l.contact_person%></td>
            <td><%=l.contact_number%></td>
            <td><%=l.from_date%> <%=l.from_ampm%></td>
            <td><%=l.to_date%> <%=l.to_ampm%></td>
            <td><%=l.total_days%></td>
            <td><%= (link_to "Click", "http://#{l.url}", target: "_blank") if l.attachment_link.present? %></td>
            <td><%=(time_ago_in_words(l.submitted_at) + " ago") if l.submitted_at%></td>
            <td><span class="badge badge-<%=l.status_leave.css%>"><%=l.status_leave.name%></span></td>
            <% if current_user %>
            <td>
                <% if [1,3].include?(l.status_leave_id) %>
                <ul class="list-unstyled">
                  <li class="mt-2"><%= link_to 'Submit', submit_leaveap_path(l), class: "btn-sm btn-info mt-2", method: 'post', remote: true %></li>
                  <li class="mt-2"><%= link_to 'Edit', edit_leaveap_path(l), class: "btn-sm btn-success mt-2", remote: true %></li>
                  <li class="mt-2"><%= link_to 'Delete', l, method: :delete, class: "btn-sm btn-danger mt-2", remote: true, data: { confirm: 'Press ok to confirm you submission.' } %></li>
                </ul>
                <% end %>
            </td>
            <% end %>